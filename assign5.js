  //QUESTION 7 : (a)
var x = [typeof x, typeof y][1];
console.log(  typeof typeof x); /* OUTPUT IS : string because the result of typeof x is "undefined" 
                                as we see the undefined is a string so the 
                                typeof "undefined" will be string. */
 

//QUESTION 1 : (B)
let a = ( "success" && "not success") || 1
console.log(a) // OUTPUT IS : not success because of && operator.
 